apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat-proxy-service-chart.fullname" . }}
  labels:
    {{- include "chat-proxy-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chat-proxy-service-chart.selectorLabels" . | nindent 4 }}
